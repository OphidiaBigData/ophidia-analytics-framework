<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE operator SYSTEM "ophidiaoperator.dtd">
<operator name="OPH_INTERCOMPARISON" version="1.0">
    <info>
        <abstract>[Type]
Data Process.

[Behaviour]
It compares two datacubes with the same fragmentation structure and
returns a new datacube as the difference (element by element) of the two.

[Parameters]
- cube : name of the first datacube to compare. The name must be in PID format.
- cube2 : name of the second datacube to compare. The name must be in PID format.
- schedule : scheduling algorithm. The only possible value is 0,
		     for a static linear block distribution of resources.
- container: name of the container to be used to store the output cube; by default it is the container of the first cube.
        
[System parameters]    
- exec_mode : operator execution mode. Possible values are async (default) for
              asynchronous mode, sync for synchronous mode with json-compliant output.
- ncores : number of parallel processes to be used (min. 1).
- sessionid : session identifier used server-side to manage sessions and jobs.
              Usually users don't need to use/modify it, except when it is necessary
              to create a new session or switch to another one.
- objkey_filter : filter on the output of the operator written to file (default=all => no filter, none => no output).
                  intercomparison : show operator's output PID as text.
        
[Examples]       
Compare the datacubes identified by the PIDs &quot;http://www.example.com/1/1&quot; and &quot;http://www.example.com/1/2&quot; :
OPH_TERM: oph_intercomparison cube=http://www.example.com/1/1;cube2=http://www.example.com/1/2;
SUBMISSION STRING: &quot;operator=oph_intercomparison;cube=http://www.example.com/1/1;cube2=http://www.example.com/1/2;&quot;</abstract>
        <author>CMCC Foundation</author>
        <creationdate>27/07/2013</creationdate>
        <license url="http://www.gnu.org/licenses/gpl.txt">GPLv3</license>
	<permission>write</permission>
    </info>
    <args>
		<argument type="string" mandatory="no" default="null">sessionid</argument>
		<argument type="int" mandatory="no" default="1" minvalue="1">ncores</argument>
		<argument type="string" mandatory="no" default="async" values="async|sync">exec_mode</argument>
		<argument type="string" mandatory="yes">cube</argument>
		<argument type="string" mandatory="yes">cube2</argument>
		<argument type="int" mandatory="no" default="0" values="0">schedule</argument>
		<argument type="string" mandatory="no" default="-">container</argument>
		<argument type="string" mandatory="no" default="all" values="all|none|intercomparison">objkey_filter</argument>
    </args>
</operator>
